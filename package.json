{
  "name": "nearsoft-kudos-graphql",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "NODE_ENV=development nodemon --exec \"node -r dotenv/config\" src/index.js",
    "now-start": "node src/index.js",
    "deploy": "now --public --dotenv --name nearsoft-kudos-graphql -e NODE_ENV=production",
    "postdeploy": "now alias nearsoft-kudos-graphql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.0.0-rc.7",
    "cloudinary": "^1.11.0",
    "dotenv": "^6.0.0",
    "graphql": "^0.13.2",
    "isomorphic-fetch": "^2.2.1",
    "mongoose": "^5.2.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.1"
  }
}
